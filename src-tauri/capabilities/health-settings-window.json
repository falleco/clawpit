{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "health-settings-window",
  "description": "Capability for the native health settings window",
  "windows": ["health-settings-window"],
  "permissions": ["core:default"]
}
